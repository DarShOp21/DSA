#include<iostream>
#include<vector>
using namespace std;

int main(){
    //CREATE VECTORS
    // vector<int>v;
    //SIZE & CAPACITY
    // cout << "Size of v : " << v.size() << endl;
    // cout << "capacity of v : " << v.capacity() << endl;
    // v.push_back(2);
    // v.push_back(7);
    // v.push_back(12);
    // cout << "Size of v : " << v.size() << endl;
    // cout << "capacity of v : " << v.capacity() << endl;
    //UPDATE A VALUE
    // v[1]=5 ;

    //INITIALIZING A VETOR
    // vector<int>v1(5,2);
    // cout << "Size of v1 : " << v1.size() << endl;
    // cout << "capacity of v1 : " << v1.capacity() << endl;
    //INSERTING A VALUE
    // v1.push_back(4);
    // cout << "Size of v1 : " << v1.size() << endl;
    // cout << "capacity of v1 : " << v1.capacity() << endl;

    //DELETING VALUE FROM VECTOR
    // vector<int>vdel;
    // vdel.push_back(2);
    // vdel.push_back(7);
    // vdel.push_back(4);
    // cout << "Size of v1 : " << vdel.size() << endl;
    // cout << "capacity of v1 : " << vdel.capacity() << endl;
    
    // vdel.erase(vdel.begin() + 1); //erasing the 2nd element in the vector
    // cout << "Size of v1 : " << vdel.size() << endl;
    // cout << "capacity of v1 : " << vdel.capacity() << endl;

    vector<int>vnew = {1,2,3,4,5};
    cout<<vnew[1]<<endl;
}